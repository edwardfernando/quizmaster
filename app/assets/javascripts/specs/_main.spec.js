import React from 'react';
import {
  expect
} from 'chai';
import {
  shallow,
  mount
} from 'enzyme';
import { spy } from 'sinon';

// import { Main } from '../components/_main.js.jsx'

describe('Main', () => {
  // it('should render Header and Body', () => {
  //   const  wrapper = shallow(<Main />);
  //   expect(wrapper.containsAllMatchingElements([ 
  //     <Header />
  //   ])).to.equal(true);
  // });
});